import isPalindrome from "../leet_Code_functions/isPalindrome";

describe("tests if a word is a palindrome", () => {
    test("aviva will return true", () => {
        expect(isPalindrome("aviva")).toBe(true)
    })
})